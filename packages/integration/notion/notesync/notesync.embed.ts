// packages/integrations/notion/notesync/notesync.embed.ts

// Simulated UI component logic (e.g., embedding preview)
export function renderNoteEmbed(noteId: string) {
  return `<iframe src="https://mock.notionsync.io/embed/${noteId}" />`;
}
